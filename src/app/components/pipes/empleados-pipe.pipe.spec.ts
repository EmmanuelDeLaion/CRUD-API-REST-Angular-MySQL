import { EmpleadosPipePipe } from './empleados-pipe.pipe';

describe('EmpleadosPipePipe', () => {
  it('create an instance', () => {
    const pipe = new EmpleadosPipePipe();
    expect(pipe).toBeTruthy();
  });
});
